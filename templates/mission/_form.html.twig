{{ form_start(form) }}
<div class="row form-group">
    <div class="col-md-12">{{ form_row(form.titre) }}</div>
    <div class="col-md-12 mt-3">{{ form_row(form.contenu) }}</div>
    <div class="col-sm-6 mt-3">
        <div class="custom-control custom-checkbox">
            {{ form_widget(form.statut) }}
            <label class="custom-control-label" for="mission_statut">Statut</label>
        </div>
    </div>
</div>
<div class="text-right mt-5">
    {{ form_widget(form) }}
    <a href="{{ path('backend_mission_index') }}" class="btn btn-outline-secondary">Annuler</a>
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
</div>

{{ form_end(form) }}
